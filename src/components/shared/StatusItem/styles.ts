import { StyleSheet } from "react-native";


const styles = StyleSheet.create({

    container:{
        borderWidth: .5,
        padding: 10,
        alignItems: 'center',
        justifyContent: 'center',
        borderRadius: 8,
    },
})

export { styles };