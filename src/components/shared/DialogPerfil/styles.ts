import { StyleSheet } from 'react-native';


const styles = StyleSheet.create({
    containerImage:{
        justifyContent: 'center',
        alignItems: 'center'
    },
    image:{
        width: '100%',
        height: 200,
        resizeMode: 'contain',
        borderRadius: 16
    }
});

export { styles };