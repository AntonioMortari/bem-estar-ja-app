import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{
        paddingHorizontal: 20,
        marginVertical: 30,
    },
    header: {
        gap: 10
    },

    buttonVoltar: {
        width: 30,
        marginBottom: 15
    },
});

export { styles }